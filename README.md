 ansible-playbook main.yaml --ask-become-pass


For HTB:

Make sure you have APP_KEY set variable of the JWT token HTB gives

 todo:
 -add configuration of foxy proxy to add localhost:8080 of burpsuite
 -install and configure burpsuite including certs
 -small script to submit the user/root flag